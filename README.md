# My Ideal Pennant Race — PHASE 1

プレイヤーが**プロ野球球団の監督兼GM**となり、選手を直接操作せず、**選手編成と采配**でチームを勝利へ導く球団経営シミュレーション。

このリポジトリは **PHASE 1（実際に遊べる最小完成版）** の実装です。

> **PHASE 1 の目的**は「見た目が完成すること」ではなく、
> 球団を選ぶ → 選手を見る → 1軍を編成する → 打順を決める → 試合をする → 勝敗が決まる →
> 成績が変わる → 順位が変わる → 日付が進む → セーブされる
> という一連のゲームループが**完全に動作すること**です。

---

## 動かし方

```bash
npm install
npm run dev          # 開発サーバー（スマートフォン縦画面 390x844 想定）
```

| コマンド | 内容 |
| --- | --- |
| `npm run dev` | 開発サーバーを起動 |
| `npm run build` | 型チェック + 本番ビルド |
| `npm test` | ドメインロジックのテスト（53件） |
| `npm run e2e` | 実ブラウザでのゲームループ通し確認（要 `npm run preview` 起動中） |
| `npm run build:single` | 1枚だけの HTML（`dist/my-ideal-pennant-race.html`）を出力 |
| `npm run verify` | 型チェック → テスト → ビルド → e2e をまとめて実行 |

`npm run e2e` は Chromium（Playwright）で実際にゲームを1周し、`e2e/shots/` にスクリーンショットを保存します。

---

## スマートフォンで遊ぶ

### A. 1枚のHTMLファイルにして持ち歩く（いちばん手軽）

```bash
npm run build:single      # dist/my-ideal-pennant-race.html が出来る
```

出力された HTML は JS も CSS も内蔵した**完全に自己完結した1ファイル**です。
AirDrop・クラウドストレージ・メール添付などでスマートフォンに送り、ブラウザで開けばそのまま遊べます。
Safari の「共有 → ホーム画面に追加」/ Chrome の「ホーム画面に追加」をしておくと、
アドレスバーのないフルスクリーンでアプリのように起動できます。

### B. 同じ Wi-Fi のPCから開発サーバーを開く

```bash
npm run dev -- --host     # 表示された Network: http://192.168.x.x:5173/ をスマホで開く
```

PC とスマートフォンを同じ Wi-Fi につないでおく必要があります。コードを直すとその場で反映されます。

### C. 静的ホスティングに置く

`npm run build` の出力（`dist/`）はどこに置いても動く静的ファイルです（相対パス出力）。
GitHub Pages / Netlify / Vercel などにそのまま配置できます。

> **セーブデータについて**
> セーブは開いたブラウザの localStorage に保存されます。
> PCとスマートフォン、あるいは別のブラウザの間でセーブデータは共有されません。
> シークレットモードやサイトデータの削除でも消えるのでご注意ください。

---

## 画面構成（スマートフォン縦画面）

下部ナビゲーション： **ホーム / 試合 / 選手 / 編成 / 順位**

| 画面 | 内容 |
| --- | --- |
| タイトル | 新規ゲーム（12球団から選択 → シーズン試合数 10 / 30 / 143 を選択）・続きから |
| ホーム | 球団名・年度・現在日付・順位・勝敗・球団総合力・次の対戦相手・次回先発投手 / 「次の試合へ」「1日進める」 |
| 試合 | 試合開始、スコアボード（1〜9回＋延長）・安打・エラー・勝敗投手、簡易実況、同日の他球団の結果、過去の試合 |
| 選手 | 選手カード一覧（全選手 / 投手 / 野手 / 1軍 / 2軍 / 成績）、タップで詳細（能力・守備適性・今季成績・1軍2軍変更） |
| 編成 | 「1軍 / 2軍」「オーダー」「先発」の3タブ |
| 順位 | リーグ別順位表（順位・球団・試合・勝・敗・分・勝率・ゲーム差）、リーグ個人成績 |

---

## PHASE 1 完成判定チェックリスト

| 完成条件 | 実装 | 確認方法 |
| --- | --- | --- |
| ゲームを起動できる | `src/main.tsx` / `App.tsx` | e2e `01-title` |
| 球団を選べる（12球団・2リーグ×6） | `domain/teams.ts` | `domain.test.ts` STEP2 |
| 選手25人がいる（投10/捕3/内7/外5） | `domain/playerGen.ts` | `domain.test.ts` STEP3 |
| 選手能力を確認できる | `PlayersScreen` / `PlayerDetail` | e2e `04`,`05` |
| 1軍/2軍を変更できる（上限31人・保有70人） | `domain/roster.ts` | `domain.test.ts` STEP6 |
| 打順を変更できる（ドラッグ＆ドロップ） | `RosterScreen` OrderTab | e2e「打順をドラッグで変更」 |
| 先発を設定できる | `RosterScreen` RotationTab | e2e「先発ローテーションを変更」 |
| 試合を開始できる／結果が表示される | `domain/simulation.ts` | `season.test.ts` STEP9 |
| 選手成績が更新される | `domain/engine.ts` | `season.test.ts`「個人成績がチーム成績と一致する」 |
| 順位表が更新される | `domain/standings.ts` | `season.test.ts`「順位表は勝率順に並ぶ」 |
| 日付が進む（戻らない） | `domain/engine.ts` | `season.test.ts`「日付は必ず進み、戻らない」 |
| 7日間制限が機能する | `domain/roster.ts` | `domain.test.ts` STEP14 |
| 再起動してもデータが残る | `domain/save.ts` | `save.test.ts` / e2e `15-reload` |

---

## 設計

```
src/
  domain/          UI に依存しない純粋なゲームロジック（すべてテスト対象）
    types.ts       Player / Team / GameState など。PHASE 2 用の拡張フィールドも定義
    rng.ts         シード付き擬似乱数（セーブに状態を保存して再現可能）
    rank.ts        能力値 1〜100 → G/F/E/D/C/B/A
    positions.ts   ポジションと守備適性ペナルティ
    playerGen.ts   架空選手の生成
    teams.ts       架空12球団（2リーグ×6球団）
    rating.ts      個人の総合評価・球団総合力
    setup.ts       オーダー／ローテーションの自動編成と修復
    roster.ts      1軍/2軍・7日間の登録変更制限
    schedule.ts    シーズン日程（10 / 30 / 143試合）
    simulation.ts  打席単位の試合シミュレーション＋簡易実況
    stats.ts       成績の集計と表示フォーマット
    standings.ts   順位表
    engine.ts      日付進行・結果反映・整合性チェック
    save.ts        localStorage セーブ／ロード
    newGame.ts     新規ゲーム生成
  ui/              React（スマートフォン縦画面）
```

### 試合シミュレーション

完全ランダムではなく、**打席ごと**に打者能力と投手能力・守備力から結果を決めます。

* 投手力：球速・制球・スタミナ・球威・変化量（スタミナが切れると能力が低下し、救援に交代）
* 打撃力：ミート・パワー（弾道は加算せず、独立した係数として長打・本塁打率に作用）
* 守備力：守備・捕球・肩（**本職と違う守備位置ではペナルティ**が入り、失策率も上がる）
* 四球 → 三振 → 本塁打 → 失策 → 安打（単打/二塁打/三塁打）→ 凡打（ゴロ/フライ/併殺/犠飛）の順で判定
* 盗塁は走者の走力と捕手の肩で成否が決まる
* 9回裏でホームがリードしていれば裏の攻撃なし。同点なら延長12回まで、決着しなければ引き分け

チーム能力が高いほど勝率は上がりますが 100% にはなりません（ゲーム内で起こりうる最大の戦力差でおよそ 6〜7 割）。

### 弾道について

弾道も他の基本能力と同じ **1〜100（G〜Aランク）** で扱います。
ただし弾道は他の能力とは意味が異なるため、**試合計算では他能力と同じ割合で加算しません**。
`domain/trajectory.ts` に独立した係数として切り出し、MatchEngine（`domain/simulation.ts`）は
この係数を通してのみ弾道を参照します。

| 係数 | 用途 | 弾道25 | 弾道50 | 弾道75 | 弾道100 |
| --- | --- | --- | --- | --- | --- |
| `homeRunFactor()` | 本塁打発生率に乗算 | 0.55 | 0.85 | 1.15 | 1.45 |
| `extraBaseFactor()` | 二塁打の出やすさに乗算 | 0.875 | 1.00 | 1.125 | 1.25 |
| `groundBallRate()` | 打球のゴロ率 | 0.65 | 0.56 | 0.47 | 0.38 |

旧仕様（1〜4の4段階）のセーブデータは読み込み時に **1→25 / 2→50 / 3→75 / 4→100** へ自動移行します
（`domain/save.ts` の `migrate()`、セーブスキーマ v1 → v2）。

---

## PHASE 2 以降への拡張

`Player.ext` に PHASE 2 で使うフィールドの入れ物を用意済みです（PHASE 1 では読み書きしません）。

```ts
ext: {
  personality, specialSkills, potential, popularity, growthRate,
  fatigue, condition, injury, contract, faStatus
}
```

セーブデータは `version` を持ち、`domain/save.ts` の `migrate()` が変換ポイントになります
（実例：v1 → v2 で弾道を 1〜4 から 1〜100 に移行）。
DH制は `League.useDH` で切り替わり、リーグごとに設定できます（オーシャンリーグがDH制）。

### PHASE 1 では実装していない機能

FA / 海外FA / ドラフト / トレード / 外国人市場 / 自由契約 / 現役ドラフト / スポンサー / 施設 /
性格 / 特殊能力 / 潜在能力 / 高度な育成 / ニュース / イベント / 歴代記録 / 監督レベル /
課金 / 広告 / オンライン対戦

**これらはUIにも配置していません**（押しても何も起こらないボタンは作らない方針）。
