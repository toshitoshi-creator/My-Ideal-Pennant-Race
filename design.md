# Design — My Ideal Pennant Race

球団管理システムの視覚言語。ここが唯一の基準で、各画面はこのファイルを読んでから実装する。
ページごとに作り直さない。システムを広げる必要があるときはこのファイルを直す。

## Genre

editorial — 野球専門誌・スコアブック・球団フロントの資料。
SaaS ダッシュボードでも、マーケティング LP でもない。

## 中心にある考え方

「Web サイト」ではなく **ゲーム内に存在する球団の記録システム**。
紙の記録用紙にインクで書かれた資料を、指で繰る。

したがって：

- 情報は「箱」に入れない。**罫線と余白**で区切る。
- 数字は資料の主役。等幅で揃え、桁を読ませる。
- 見出しは小さく、静かに。大きいのは中身のほう。
- 色は装飾ではなく**意味**。

## Theme — ink on paper

紙（生成りのオフホワイト）に墨で刷った記録用紙。暗い SaaS 画面からの完全な離脱。

```
--paper      oklch(96.2% 0.008 85)    生成りの紙
--paper-2    oklch(93.4% 0.010 85)    敷き紙（表の縞・沈んだ面）
--paper-3    oklch(89.5% 0.012 85)    罫線より濃い面
--ink        oklch(24.5% 0.012 265)   墨（本文・数字）
--ink-2      oklch(46.0% 0.010 265)   補助情報
--ink-3      oklch(62.0% 0.008 265)   さらに弱い情報
--rule       oklch(84.0% 0.010 85)    罫線
--rule-2     oklch(71.0% 0.012 85)    強い罫線（見出し下）
--accent     oklch(43.5% 0.148 26)    深い赤 — 球団色・強調・警告
--accent-ink oklch(98% 0.005 85)      赤地の上の文字
--grass      oklch(46.0% 0.085 148)   芝の緑 — 成長・好調・成立
--brass      oklch(58.0% 0.105 78)    真鍮 — タイトル・記録・日本一
--focus      oklch(43.5% 0.148 26)    フォーカスリング（accent と同値）
```

**紙の帯**: light（L > 85%）／**表示体**: system-sans（等幅数字）／**アクセント色相**: warm（26°）

### 色の役割（§6）

| 色 | 意味 |
| --- | --- |
| accent（深い赤） | 危険・怪我・成績下降・契約警告・整理候補 |
| grass（緑） | 成長・好調・回復・契約成立・育成候補 |
| brass（真鍮） | タイトル・MVP・優勝・特別記録 |
| ink | 基本情報 |
| ink-2 / ink-3 | 補助情報 |

**色だけで状態を表さない。** 必ず記号（▲▼→）・ラベル・数値のいずれかを添える。

## Typography

外部フォントは**読み込まない**。単一 HTML ファイルとして配布するゲームなので、
オフラインで確実に動くことを font の目新しさより優先する。
そのぶん、ウェイト・字間・大小・字種・等幅数字で階層を作る。

- 本文: `-apple-system, 'Hiragino Sans', 'Noto Sans JP', 'Yu Gothic', sans-serif`
- 数字: 同じスタックに `font-variant-numeric: tabular-nums`（成績表で桁が揃う）
- ラベル: 大文字ラテン + `letter-spacing: .14em` + 11px + ink-3（スコアブックの欄名）
- 見出し: 太さ 800 / `letter-spacing: -.01em`。サイズではなく**太さと余白**で立てる
- 特大数字: `--text-figure`（36px / 800 / tabular）— スコア・順位・年俸

## Spacing

4pt スケール。`--space-3xs`〜`--space-3xl`。生の値は書かない。

## 見出し（PHASE 4.3）

画面の節は「球団資料の欄」として立てる。上に短い赤い罫、
その下にラテン語の欄名、その下に日本語の見出し。

```
▬▬
CLUB REPORT
球団レポート
```

* ラテン語は資料の欄名（11px・大文字・字間 .14em・薄墨）
* 日本語がその欄の実際の名前。**英語だけにして分からなくしない**（§3）
* 赤い罫は 18px の短い線。初回だけ左から伸ばす（§21）。2回目以降と reduced-motion では伸ばさない

用語は野球の球団業務に寄せる：

| 画面の中身 | 欄名 |
| --- | --- |
| 選手詳細 | PLAYER REPORT |
| 分析 | SCOUT REPORT |
| 扱いの助言 | GM RECOMMENDATION |
| チーム分析 | CLUB REPORT |
| ニュース | CLUB NEWS |
| 歴史 | CLUB HISTORY |
| 記録 | RECORD BOOK |
| 契約更改 | CONTRACT DESK |
| FA | FREE AGENCY |
| トレード | TRADE DESK |
| ドラフト | DRAFT ROOM |

## 案件票（PHASE 4.4）

GM が判断する案件は、角丸のカードではなく **左に罫を引いた回覧資料**として置く。
1 枚目だけ罫を赤くし、余白を広く取る。2 枚目以降は静かに。机の上は最大 3 枚まで。

```
│ GM NOTE                        07.18
│ 中継ぎ陣に疲労が蓄積しています
│
│ CURRENT SITUATION  いまの状況
│ DATA               数字
│ SCOUT NOTE         見方
│ SHORT TERM / LONG TERM （1 枚目のみ）
│ OPTIONS            決められる場所
```

* 案件は **問題を知らせるだけ**。押しても画面が移るだけで、何も決まらない
* `OPTIONS` は必ず「実際に決められる画面」への導線。行き先の無い選択肢は置かない
* 「おすすめ」「最適」「AI」「正解」は書かない。書くのは DATA / REASON / TRADEOFF / RISK

追加する欄名（英字＋日本語のセットは §3 のまま）：

| 画面の中身 | 欄名 |
| --- | --- |
| 今日の判断材料 | GM NOTE |
| 案件ひとつ | CURRENT SITUATION / DATA / SCOUT NOTE / OPTIONS |
| 短期と長期 | SHORT TERM / LONG TERM |
| 判断を記録した | DECISION RECORDED |
| 試合前 | PRE-GAME BRIEF |
| 試合後 | POST GAME（KEY MOMENTS / PLAYER NOTE / TEAM NOTE） |
| 選手の立ち位置 | PLAYER STATUS |
| 成績と成長 | CURRENT PERFORMANCE / DEVELOPMENT / POTENTIAL OUTLOOK |
| 強みと弱み | STRENGTH / WEAK POINT |
| 球団の課題 | TEAM REPORT（WHY / DATA） |
| 補強の検討 | CONSIDER（OPTIONS / COST / RISK） |
| 一日の流れ | TODAY |
| シーズンの記録 | SEASON TIMELINE |
| 判断の記録 | GM JOURNAL |

## 判断の見せ方（PHASE 4.4）

決めた瞬間に結果・影響・数字を一度に出さない。

```
DECISION RECORDED   判断を記録しました
     ↓ 260ms
何を決めたか
     ↓ 260ms
次にどこを見るか
```

ゲームの状態はボタンを押した時点ですでに確定していて、
この表示はその写しでしかない。飛ばしても・リロードしても結果は変わらない。

## 情報の並び順（PHASE 4.3）

資料は **判断 → 根拠 → データ** の順に読ませる（§19）。
能力値やグラフを先に出さない。

## 大きさの不均一さ（§25）

同じ大きさで並べない。編集された新聞のように、

* 重要 — 大きく、余白を広く、罫線を太く
* 通常 — 標準
* 補足 — 小さく、薄く

の三段にする。ただし無秩序にはしない。

## 面の使い分け（§2 — カード禁止）

情報は原則 **section**（罫線 + 余白）で区切る。角丸の箱は例外。

| 使うもの | いつ |
| --- | --- |
| `.section` | 既定。上に罫線、見出しは小さなラベル。**枠も影も角丸も無い** |
| `.sheet-panel` | プレイヤーの判断を求める箱だけ（GM 判断・イベント）。左に太い赤帯 |
| `.stat-line` | ラベルと値の行。罫線で区切る |
| `.record-table` | 成績表。等幅数字・縞 |
| `.scoreboard` | 試合のスコア |
| `.player-row` | 選手一覧の行 |

**箱の中に箱を入れない。**

## Motion

- easing: `--ease-out: cubic-bezier(.2,.7,.3,1)` / `--ease-in-out`
- 画面遷移: 不透明度のみ 140ms。位置は動かさない（押し間違いを防ぐ）
- 「開くたびに全カードがふわっと出る」演出は**禁止**（§16）
- アニメーションは**情報の順序**にだけ使う（§17）
- 2回目以降は短縮。`prefers-reduced-motion` で即時表示

## 数字アニメーション（§18）

意味のある変化だけ。スコア・年俸・資金・契約総額・球団評価。
一覧の数字はカウントアップしない。

## 各画面が必ず共有するもの

- 紙とインクの色、赤・緑・真鍮の意味
- ラベル（大文字ラテン + トラッキング）の書式
- 罫線の太さ（hairline = 1px `--rule`、見出し下 = 2px `--rule-2`）
- 数字の等幅
- ボタンの形（角丸 3px。ピル型もグラデーションも使わない）

## 画面ごとに変えてよいもの

- セクションの順序と面積配分（情報の重要度で決める。均等に扱わない）
- 見出しの有無
- 表・スコアボード・タイムラインのどれを使うか

## 選手のビジュアル（PHASE 4.5）

> **Players are assembled, not selected.**
> 選手画像を「選ぶ」のではなく、選手を「部品から組み立てる」。

1枚の完成画像を大量に用意すると、300〜800人の選手には足りず、
必ずどこかで同じ顔が並ぶ。そこで顔・体を独立した部品に分け、
`playerId` から決まる組み合わせとして毎回その場で描く。

### 決まりごと

* **同じ選手は、どの画面でも同じ人物**。一覧・詳細・ニュース・ドラフト・トレードで一致する
* 部品の選び方は `hash("player-appearance-v1:" + player.id)` だけで決まる。
  ゲームの乱数（`rngState`）は読まないし、進めない
* 見た目はゲームの判断を決めない。**顔の良し悪しと能力に関係はない**
* 画像はセーブしない。`playerId` から作り直せる
* 外部の画像・CDN・Webフォントに依存しない。オフラインで完結する
* Canvas も WebGL も使わない。すべて SVG

### 部品

| 部品 | 種類 |
| --- | --- |
| 頭の形 | 10 |
| 髪型 | 15 |
| 眉 | 10 |
| 目 | 12 |
| 鼻 | 10 |
| 口 | 10 |
| 耳 | 6 |
| あご・頬 | 6 |
| 体型 | 10 |
| 肌 | 8 |
| 髪色 | 8 |
| ひげ | 8 |
| 表情 | 10 |
| ポーズ | 8 |

### 座標系

すべての部品が同じ座標系（`viewBox 0 0 256 320`）の上に乗る。

```
y=  20  帽子の頂点
y=  52  頭頂
y=  76  帽子のつば
y= 124  目
y= 150  鼻
y= 178  口
y= 200  あご先
y= 214  首
y= 246  肩
```

顔の中心は常に `x=128`。顔の部品は「頭の形」が返す寸法
（`halfWidth` / `chinY` / `eyeGap` など）を見て位置を決めるので、
どの輪郭にどの部品を載せても輪郭からはみ出さない。

### 描く順番

```
背景 → 体 → ユニフォーム → 腕 → 首 → 耳 → 頭 → 髪(後)
→ 頬・あご → 年輪 → 眉 → 目 → 鼻 → 口 → ひげ → 髪(前) → 帽子 → 小物
```

### 線の階層

| 用途 | 太さ |
| --- | --- |
| 外形（頭・体・帽子） | 3 |
| まぶた | 2.6 |
| 内部のディテール（鼻・耳・あご） | 1.6〜2 |
| 年輪 | 1.6 |

`vector-effect: non-scaling-stroke` を全体にかけ、
40px でも 260px でも線の太さが変わらないようにしている。

### 色

肌8段階・髪8色は、いずれも紙とインクの世界に合わせて彩度を抑えてある。
**色だけを変えて「別人」とはしない**。輪郭・目・鼻・口・髪型が変わって初めて別人になる。

球団色を使うのは帽子・襟・ラインだけで、背景を球団色で塗りつぶさない。

### 年齢

同じ選手が年を取っても、輪郭・目・鼻・口・耳・肌は動かない。
変わるのは髪型・髪色・ひげ・年輪だけ。

| 段階 | 年齢 | 変わること |
| --- | --- | --- |
| YOUTH | 〜22 | ひげが出にくい |
| PRIME | 23〜27 | 基本の形 |
| MATURE | 28〜31 | ひげが出やすくなる／年輪が入りはじめる |
| VETERAN | 32〜35 | 生え際が上がることがある／白髪が出はじめる |
| ELDER | 36〜 | 白髪・薄毛が増える／年輪2本 |

### 表情

表情は「すでにゲームが決めた状態」を映すだけで、ゲームの判断には関わらない。

```
怪我 → injured
スランプ → disappointed
疲労 → tired
絶好調 → confident
好調 → focused
新人 → focused
それ以外 → neutral
```

## 選手の画像素材（PHASE 4.6）

> **AIで作った素材を、AIを呼ばずに使う。**

PHASE 4.5 の SVG を土台に残したまま、外部の画像生成AIで作った素材を
そのまま重ねられるようにした層。**素材が1枚も無い状態が正常な状態**であり、
そのときは PHASE 4.5 の SVG がそのまま描かれる。

### 決まりごと

* ゲーム本体は画像生成AIを呼ばない。APIキーも `.env` も要らない
* 画像はビルド時に同梱する。起動後に CDN・外部URL・外部APIへ出ていかない
* 素材が欠けても、読み込みに失敗しても、**穴も壊れた画像アイコンも出さない**
* 素材の選び方は `hash("player-appearance-v2:" + player.id)` だけで決まる。
  v1（SVG）とは別系列なので、片方を足してももう片方の顔は動かない
* 球団色は素材に焼き込まない。ゲーム側が細い線として重ねる
* 実在の選手・実在球団のロゴ・既存ゲームのキャラクターに似せない

### 3段階

```
1. 画像素材                 ← 必須11種類がすべてそろっているとき
2. 画像素材の別解像度       ← 1枚でも読み込みに失敗したとき
3. PHASE 4.5 の SVG        ← 素材が足りない / 全部駄目なとき（いまはここ）
```

入口は `PlayerVisual` ひとつ。使う側は `Player` を渡すだけでよい。

### 共通キャンバス

すべての素材が同じ 1024 × 1280 の上に、同じ基準点で描かれる。
だから重ねるだけで顔が組み上がり、実行時の位置合わせが要らない。

```
y= 210  頭のてっぺん
y= 306  帽子のつば
y= 428  眉      （左 x=404 / 右 x=620）
y= 496  目      （左 x=408 / 右 x=616）
y= 512  耳      （左 x=250 / 右 x=774）
y= 600  鼻
y= 712  口
y= 740  ひげ
y= 800  あご先
y= 856  首の付け根
y= 980  用具
```

顔の中心は常に `x=512`。正本は `config/visual-assets.json`。

### 重ねる順番

```
姿勢 → 体 → ユニフォーム → 首 → 耳 → 頭 → 後ろ髪 → あご
→ 眉 → 目 → 鼻 → 口 → ひげ → 髪 → 帽子 → 眼鏡 → 用具 → 表情
```

耳が頭より下、帽子が髪より上、姿勢が最下層、表情が最上層。
この順番も `config/visual-assets.json` の `layer` が正本で、テストが実装と突き合わせる。

### 種類と点数

| 種類 | 点数の目安 | 必須 |
| --- | --- | --- |
| 頭 / 髪 / 眉 / 目 / 鼻 / 口 | 10〜20 | ○ |
| 耳 / あご / 首 | 6〜10 | ○ |
| 体 | 8〜12 | ○ |
| ユニフォーム | 1〜8 | ○ |
| ひげ | 8〜12 | |
| 帽子（帽子・ヘルメット・マスク） | 3 | |
| 眼鏡 | 0〜10 | |
| 用具 / 表情 / 姿勢 / 後ろ髪 | 任意 | |

部品は全選手で共有するので、**選手が何人いても容量は増えない**。

### 守備位置から決まるもの

| 守備位置 | 構え | かぶり物 | 用具 |
| --- | --- | --- | --- |
| 投手 | 投球 | 帽子 | グラブ |
| 捕手 | 構え | マスク | ミット・プロテクター |
| 内野 | 守備 | 帽子 | グラブ |
| 外野 | 守備 | 帽子 | グラブ |
| 打者 | 打席 | ヘルメット | バット |

### 素材を作る人へ

`assets/prompts/` に絵柄の決めごと（Style Bible）・ネガティブ・
18種類ぶんのプロンプト・取り込み手順を置いている。ゲームには含まれない。


## Exports

### tokens.css

`src/styles.css` の `:root` がそのまま tokens。ここを直せば全画面に伝わる。
